{#
/**
 * @file
 * Unformatted list override for the National Tours block display
 * (view: tours, display: national).
 *
 * Prints rows with no extra wrapper markup — parent page template
 * provides the flex/snap slider container (#national-slider). Remember
 * the `display: contents` CSS fix on .views-element-container and
 * [class*="js-view-dom-id-"] is required for this to lay out correctly.
 */
#}
{% for row in rows %}
  {{- row.content -}}
{% endfor %}
