{#
/**
 * @file
 * Unformatted list override for the International Tours block display
 * (view: tours, display: international).
 *
 * Prints rows with no extra wrapper markup — parent page template
 * provides the flex/snap slider container (#intl-slider). Remember the
 * `display: contents` CSS fix on .views-element-container and
 * [class*="js-view-dom-id-"] is required for this to lay out correctly.
 */
#}
{% for row in rows %}
  {{- row.content -}}
{% endfor %}
